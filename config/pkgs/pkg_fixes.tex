% ------------------------------------------------------------------------
% config/pkgs/pkg_fixes.tex
%
% Resolve conflicts between packages and the document class.
% ------------------------------------------------------------------------

% Fix class 'memoir' conflicts with packages.

\let\Cref\relax  % with package 'cleveref'
\let\ordinal\relax  % with package 'datetime'
\let\newfloat\relax  % with package 'floatrow'


% Fix package conflicts.

%% Load package 'lmodern' early to resolve font size substitutions which
%% occur with the default Computer Modern fonts.  Fix any resulting
%% package conflicts.
\usepackage{lmodern}
\let\mathbf\relax  % with package 'newtxmath'
